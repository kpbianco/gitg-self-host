# M6H-01 software evidence handoff

Date: 2026-08-27
Baseline: `4301b4f2ac8010dd4a25a85e9143bc5b726937c3`
Branch: `codex/m6h-01-weekly-execution`

## Implemented boundary

- Authenticated Personal OS → context priority → current practice → one weekly
  action → submitted proof → structured review loop.
- Immutable append-only plan revisions and one review per reviewable plan.
- Exact proof selection after plan creation, inside the seven-day window, and
  no later than the frozen review timestamp.
- Privacy-safe deterministic readiness and additive migration.
- No evidence creation, score write, recommendation factor, practice
  completion, mastery state, telemetry, or Personal OS text analysis.
- No human-review, specialist, participant, release, or deployment gate is
  claimed or required by this software batch.

## Verification record

- `tests/test_weekly_execution.py`: 6 passed.
- Personal OS, practice, deployment-contract, M6C readiness, and auth
  regression set: 49 passed.
- `make m6h-weekly-check PYTHON=.venv/bin/python`: passed against isolated
  migrated state with 383 runtime/score-active protocols and one exact proof
  event.
- Local Playwright execution: unavailable because this workspace has no
  Chromium binary. The new journey collected successfully; hosted browser
  status remains pending.
- Local Compose execution: unavailable because this workspace has no Docker
  executable. Hosted recovery-drill status remains pending.
- First complete non-browser run: 356 passed and one migration preservation
  assertion failed because its historical digest included the new empty weekly
  tables before rolling back to migration 0007. No product behavior test
  failed; the additive-table exclusion is being corrected and will be rerun.
- Focused context and weekly migration round trips after repair: 2 passed.
- Final complete non-browser rerun: 357 passed, 12 browser tests deselected,
  in 49 minutes 15 seconds.
- `make pilot-check PYTHON=.venv/bin/python`: passed independently after the
  complete suite, confirming 383 runtime-active and 383 score-active protocols,
  1,151 actions, zero evidence events, and one replayed score-state run.
- `./scripts/agent-verify.sh full`: contract checks and a second complete
  non-browser run passed (357 passed, 12 browser tests deselected, in 48 minutes
  36 seconds). The workspace execution transport disconnected during the
  following readiness sequence, so no aggregate local result is claimed;
  hosted CI remains the authoritative uninterrupted aggregate.
- Hosted quality timeout increased from 75 to 90 minutes without removing or
  weakening a check, providing runner-variance headroom for the 357-test suite
  plus nine isolated readiness gates.
- Hosted run 93 executed Chromium successfully: 11 existing journeys passed,
  while the new weekly journey exposed horizontal overflow from native form
  control minimum sizing at 200% zoom on a 390-pixel viewport. The candidate
  now permits those controls and responsive grid tracks to shrink to their
  container; the overflow assertion also reports exact offending elements on
  future failures. A final hosted rerun is required.

Exact final candidate commit, full-suite result, hosted workflow, browser, and
Compose results remain to be recorded after the candidate tree is frozen.
