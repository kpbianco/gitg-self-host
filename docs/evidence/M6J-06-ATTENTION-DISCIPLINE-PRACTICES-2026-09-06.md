# M6J-06: attention and discipline content continuation

The owner asked to continue making each competency rich, self-contained and
representative of its underlying goal. Fresh repository retrieval found the
latest saved M6J-05 candidate at
`c3491a4ff4c0ba77c7d8f2244bae7218ae568880` on
`codex/m6j-05-emotional-resilience-practices`, including the earlier domain 06
browser-label correction. This is the baseline for the continuation; the
parallel older emotional-maturity branch is preserved rather than overwriting
the more recent candidate.

## Delivered content and honest coverage

Ten domain 08 competencies now have individually authored runtime exercises:
08.01 and 08.03–08.11. Their 30 distinct action instructions, 120 observable
checks and 40 worked outcome examples bring runtime authoring to **115/383**,
with exactly **268 rewrite-pending**. All prior 105 rewrites are retained.

The complete text is in `docs/authoring/M6J-06-PRACTICE-READER.md`; every
canonical scope and evidence-of-progress goal is explicitly mapped in
`docs/authoring/M6J-06-SCOPE-MAP.md`. Default examples include a sampled
attention audit, a reversible impulse choice, an ordinary desire comparison,
an actionable reminder, repeated starts, a notebook/departure arrangement,
normal and minimum learning outputs, optional simplification, a four-part
invitation and a flexible reading/conversation rule.

Each practice supplies a purpose, materials, concrete default, burden,
distinct ordered tasks, observable checks, adaptations, review and four
outcome dispositions. Necessary supports and consent remain intact; there is
no deliberately provoked pain, fatigue, craving, food restriction, sleep loss
or compulsory hardship. A lack of discomfort or low motivation is recorded
honestly instead of manufacturing the desired test condition. These are
original educational designs, not clinical protocols or efficacy claims.
No new external factual sources or expert acceptance records were added.

## Frozen legacy compatibility

Competency 08.02 receives a full separately validated source-only guide in
`docs/authoring/legacy-guides/08.yaml`: three explanatory actions, 12 reading
checks and four examples. It explains the same usual/changed/repeat windows,
15-minute comparison and seven-day repeat. It does not redefine the
`practice-observation-v1` markers, runtime wording or completion rules.

The authoring loader permits explicit guide-only dispositions solely for the
five known frozen legacy competency IDs. It requires complete, matching,
nonduplicate guide files, rejects nonlegacy or foreign exclusions, and checks
instruction uniqueness across guides and runtime exercises. The guide is
excluded from runtime projection. `apply_exercise` still rejects a legacy
protocol, and coverage keeps 08.02 `rewrite_pending`, listing its guide in a
separate field. This is an explicit compatibility design, not a waiver of
missing content or a claim that all eleven runtime packages were rewritten.

## Executed verification

Canonical content hash:
`bfe86586716f3dcda49d1939d9ac91ea6d44228a93b9bec78734e3a193225514`.

The deterministic compiler check passed with 115 authored and 268 pending.
An independent comparison confirmed that exactly ten intended runtime
packages changed and the other **373 are byte-identical**, including frozen
08.02. All 383 protocol identities and parent mappings, 1,151 action IDs and
completion rules are unchanged. The reader contains every authored field for
all eleven entries exactly. Assessment, activation, ranking, scoring and
participant data are outside the changed paths.

The initial focused authoring/import/closeout/replay suite passed 31 tests in
254.11 seconds before the retained-typed-evidence correction described below.
Final-head focused results are recorded separately. It includes new minimum/full closeout cases for impulse
choice, habit design and commitment closeout, plus explicit rejection tests
for missing, nonlegacy, duplicate and foreign guide declarations. A separate
test proves a guide remains runtime-pending and cannot be applied to the
legacy evidence contract. Existing cases remain in the suite.

All **33 browser journeys collect**. Three new journeys cover impulse choice,
habit design and commitment closeout, retaining previous journeys and overflow
assertions. Their text and action-scoped checks were compared with the final
authoring inputs. Collection is not browser execution or visual review.

The first report-generation attempt caught a real pre-frontier compatibility
error: `baseline_start_occurred: input kind does not match its rule`. The
08.06 primary markers had been changed into artifact checks, conflicting with
the existing M6D Boolean/count fixtures. The correction adds 08.06 to the
retained-typed-evidence contract, restores its original rule source, and
rewrites its three actions to follow the original usual start, one-cue start
and comparison sequence. All original primary, supporting, adverse, duration
and count rules now compare exactly with the baseline. No fixture or gate was
weakened. An intermediate test rerun was stopped before restoring the source;
it is not passing evidence. The final candidate is rechecked separately.

The corrected candidate's focused run passed 31 cases in 251.07 seconds and
exposed one test-helper assumption: its hypothetical future revision attached
artifact criteria to the retained Boolean rule. The helper now makes a valid
Boolean predicate revision for that case while retaining the previous
artifact revision for the other cases. The corrected 08.06 full-closeout and
historical-replay case passed separately in 21.33 seconds (31 deselected).
Thus all 32 focused cases have passing evidence across the suite and targeted
correction rerun; the aggregate suite was not rerun after this test-only fix.
The original M6D competency-evidence report now generates successfully with
unchanged golden fixtures. The final coverage/originality/risk, catalog governance, composite scoring
catalog and competency-evidence generation targets all completed successfully.
Every catalog-hash-bearing report uses the final content hash; the baseline
and both intermediate hashes are absent from current reports. Human review
remains zero, and 08.02 remains pending with a separate guide disposition.

The final repository contract passed at 2026-09-06T22:46:44+00:00: 907-file manifest,
Ruff format/lint, Django system checks and migration-drift check. All 37
changed paths satisfy the explicit allowed/forbidden scope and diff whitespace
checks. The final evidence-only edit is followed by manifest regeneration and
verification. The saved branch is transferred using verified Git blob hashes
and an exact local/remote Git tree comparison; these checks do not replace
unexecuted release gates.

## Unverified acceptance and residual work

No Chromium binary is present in the configured/default Playwright cache, and
Docker is unavailable. Browser execution, final screenshot inspection and
Compose backup/restore/recreation are therefore unverified. The full pytest
suite, isolated pilot checks, all readiness gates and hosted aggregate CI are
not established by these focused checks. No merge or deployment occurs in
this content continuation, and no release readiness is claimed.

Human semantic, accessibility, cultural, safety, measurement, clinical and
formal source-completeness review remain open. `ER-M6A-003` stays pending,
`RG-M6A-002` stays open and all empirical calibration axes remain incomplete.
The 268 pending runtime rewrites include 08.02 despite its complete guide.
Continue with domain 09 while retaining explicit legacy compatibility work;
do not describe the original combined content and validation concerns as
fully resolved.
