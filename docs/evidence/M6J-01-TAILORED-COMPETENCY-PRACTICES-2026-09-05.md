# M6J-01 tailored competency practices

Status: local implementation; full verification and publication in progress.

The owner's requirement covers all 383 competencies. This first cohort
rewrites exactly 42: 01.01–01.11, 02.01–02.15 and 13.01–13.16. There are 341 remaining
rewrite-pending practices and zero human-review-complete claims. This is a
cohort delivery, not resolution of the entire content roadblock.

Each exercise has an explicit goal, materials or prerequisites, usable default
case, ordered actions, observation checks, four worked evidence outcomes,
adaptations, burden, review and a statement of its narrower relationship to
the canonical competency. Original authoring inputs compile into the existing
manifest-listed canonical YAML; the runtime does not read the authoring files.

The rewrite changes 127 action instruction sets while preserving all 383
protocol identities, 1,151 action identities, parent mappings, activation and
Decision 053 mathematics. The earlier home-upkeep protocol retains its exact
typed rules and M6D golden fixture expectations. Its initial rewritten rules
were rejected by fixture validation and were restored before publication;
new instructions explain its existing audit, plan, installation and upkeep
checks. All five legacy runtime projections remain frozen.

Import checks all active and paused practice dependencies before changing any
protocol. An affected ongoing practice causes a diagnostic and no instruction
replacement. Completed or explicitly stopped work allows subsequent content
revisions; historical event snapshots keep their original criteria. Older
completed practice pages still refer to current catalog display text; this
batch does not introduce a full historical instruction snapshot model.

Thirteen inspected external references support narrow philosophical, garment-care,
cleaner, food-safety, flatbread-technique and plant-care statements. They do not
validate the product-authored exercises. References are available from setup.
No clinical efficacy, broad reliability, psychometric validity, specialist
acceptance or physical execution is claimed. ER-M6A-003 remains pending,
RG-M6A-002 remains open, and all eight calibration evidence axes remain open.

## Verification evidence

- Eight focused tests passed after retaining the home-upkeep rules. They cover
  coverage honesty, duplicate/malformed input rejection, runtime content
  projection, authenticated rendering, active/paused import protection,
  minimum/full human closeout, baseline immutability and replay after a later
  catalog revision.
- Ruff lint passed. Full suite, deterministic report freshness, desktop/mobile
  Playwright, Compose and hosted aggregate readiness remain pending here until
  their results are recorded.
- PR #52 initially contained 27 practices. Its first hosted browser run passed;
  visual review of artifact 9963478531 found cramped mobile checkbox groups.
  A follow-up uses fieldsets, legends and full-width criteria groups. Ethics
  was then incorporated into this same first delivery, so the final 42-practice
  head requires a fresh complete CI run and screenshot review before merge.
- Validation is software and synthetic workflow evidence. No participant,
  editorial-human, specialist, household-field or production deployment
  evidence has been produced by this batch.

## Continuation

Use `tailored_practice_coverage_v1.json` to select the next exact cohort; domain
03 is next. Every newly declared domain must contain all of its canonical IDs.
Do not infer exercise content from labels, classify revised packages by an old
provenance sentence, or report a passed schema as editorial acceptance.
