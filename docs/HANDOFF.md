# Concise handoff

Grounded Growth is a self-hosted Django guided-development application with
383 canonical practices, immutable assessment/evidence history and explicit
human-closeout completion credit. See `docs/PROJECT_HANDOFF.md` first.

PR #52 merged at `e993ad142dee7652c3c029d6b1c75c0cdaf3d298`. M6J-02 continues
on `codex/m6j-02-self-knowledge` with all 12 practices in domain 05. It adds
36 distinct action instructions, bringing authored coverage to 54/383 and
leaving 329 rewrite-pending. Domain 03 remains the proposed next cohort.

Read `contracts/active-batch.yaml`, ADR 0018, the authoring contract and
`docs/evidence/M6J-02-SELF-KNOWLEDGE-PRACTICES-2026-09-05.md` for exact scope
and evidence. The compiler reads explicit exercises under `docs/authoring/`
and emits the canonical practice YAML. It must not invent missing content.

The 42 prior authored practices, five legacy projections, home-upkeep rules,
all stable IDs, activation records and Decision 053 scoring/ranking/closeout
behavior stay unchanged. Submitted events retain their own replay snapshots;
imports reject changed instructions for active or paused work.

All required CI and the final desktop/mobile browser artifact must pass and
be reviewed before the owner-authorized merge. A cancelled or missing check
does not count. The cancelled PR #52 run is not validation of merged main;
its separate main run is 33976905386. Do not deploy or modify participant data.

Editorial and specialist review, formal source completeness and all eight
empirical calibration axes remain open. Two new inspected research sources
support narrow descriptive findings; they do not validate these exercises.
